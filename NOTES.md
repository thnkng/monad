você está usando js, deve usar somente o css.

        // Active link highlighting on scroll
        const sections = document.querySelectorAll('main section[id]');
        const navLinks = document.querySelectorAll('.sidebar-link');

        Não faça isso, porque o dev pode esperar uma coisa e quando usar o estilo não conseguir o resultado

        tem que usar 100 o css e js, se não der, temos que rever e melhorar o design syuste, entendeu??

        vi que você usou um monte de customizações via o docs.css. Isso ou tem que estár em layout ou temos que arrumar os atoms e o restante!

        outra coisa, os links e is de menu deve ter como pad~roa as cores thought-focus.

        Estou achando a fonte padrão muito grande, acho que pode ser um pouco menor, senão qualquer conteudo tera miota barra de rolagem, não?

        Liste o que você entendeu!
